# Sample workflow for building and deploying a Hugo site to GitHub Pages
name: Deploy Hugo site to Pages

on:
  workflow_dispatch:
  push:
    branches:
      - main

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

# Allow only one concurrent deployment, skipping runs queued between the run in-progress and latest queued.
# However, do NOT cancel in-progress runs as we want to allow these production deployments to complete.
concurrency:
  group: "pages"
  cancel-in-progress: false

# Default to bash
defaults:
  run:
    shell: bash

jobs:
  # Build job
  build:
    runs-on: ubuntu-latest
    env:
      HUGO_VERSION: 0.135.0
    steps:
      - name: Install Hugo CLI
        run: |
          wget -O ${{ runner.temp }}/hugo.deb https://github.com/gohugoio/hugo/releases/download/v${HUGO_VERSION}/hugo_extended_${HUGO_VERSION}_linux-amd64.deb \
          && sudo dpkg -i ${{ runner.temp }}/hugo.deb          
      - name: Install Dart Sass
        run: sudo snap install dart-sass
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive
          fetch-depth: 0
      - name: Update submodules
        run: |
          echo "Initial submodule status:"
          git submodule status || echo "No submodules found"
          
          echo "Syncing and updating submodules..."
          git submodule sync --recursive
          git submodule update --init --recursive --force
          
          echo "Final submodule status:"
          git submodule status
          
          echo "Checking themes directory structure:"
          ls -la themes/ || echo "Themes directory not found"
          ls -la themes/PaperMod/ || echo "PaperMod directory not found"
          
          # Verify specific critical files
          echo "Checking for critical theme files:"
          [ -f "themes/PaperMod/theme.toml" ] && echo "✅ PaperMod theme.toml found" || echo "❌ PaperMod theme.toml missing"
          [ -f "themes/PaperMod/layouts/_default/baseof.html" ] && echo "✅ baseof.html found" || echo "❌ baseof.html missing"
      - name: Setup Pages
        id: pages
        uses: actions/configure-pages@v4
      - name: Install Node.js dependencies
        run: "[[ -f package-lock.json || -f npm-shrinkwrap.json ]] && npm ci || true"
      - name: Verify theme installation
        run: |
          echo "=== THEME VERIFICATION ==="
          echo "Verifying PaperMod theme is properly installed..."
          
          if [ ! -f "themes/PaperMod/theme.toml" ]; then
            echo "❌ ERROR: PaperMod theme.toml not found!"
            echo "Available files in themes/:"
            ls -la themes/ || echo "Themes directory completely missing"
            echo "Available files in themes/PaperMod/:"
            ls -la themes/PaperMod/ || echo "PaperMod directory completely missing"
            exit 1
          fi
          echo "✅ PaperMod theme.toml found"
          
          if [ ! -f "themes/PaperMod/layouts/_default/baseof.html" ]; then
            echo "❌ ERROR: Required theme baseof.html not found!"
            echo "Available files in layouts directory:"
            ls -la themes/PaperMod/layouts/ || echo "Layouts directory missing"
            exit 1
          fi
          echo "✅ Required theme layouts found"
          
          # Test if Hugo can see the theme
          echo "Testing Hugo theme detection..."
          hugo list all --quiet > /dev/null 2>&1 || {
            echo "❌ ERROR: Hugo cannot properly detect the theme"
            hugo version
            echo "Hugo configuration:"
            cat hugo.toml
            exit 1
          }
          echo "✅ Hugo can detect the theme properly"
          echo "=== THEME VERIFICATION COMPLETE ==="
      - name: Pre-build verification
        run: |
          echo "=== FINAL PRE-BUILD CHECK ==="
          echo "Working directory: $(pwd)"
          echo "Hugo version: $(hugo version)"
          echo "Theme configuration in hugo.toml:"
          grep -A 5 -B 5 "theme" hugo.toml || echo "No theme config found"
          echo "Final check - critical files exist:"
          [ -f "themes/PaperMod/layouts/_default/baseof.html" ] && echo "✅ baseof.html still present" || echo "❌ baseof.html disappeared!"
          [ -f "themes/PaperMod/theme.toml" ] && echo "✅ theme.toml present" || echo "❌ theme.toml missing!"
          echo "Content directory structure:"
          ls -la content/ || echo "No content directory"
          echo "=== STARTING HUGO BUILD ==="
      - name: Build with Hugo
        env:
          # For maximum backward compatibility with Hugo modules
          HUGO_ENVIRONMENT: production
          HUGO_ENV: production
        run: |
          hugo \
            --gc \
            --minify \
            --baseURL "${{ steps.pages.outputs.base_url }}/"          
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./public

  # Deployment job
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
