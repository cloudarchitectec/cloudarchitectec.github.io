{{- /* PaperMod rss.xml + cover.image as media:content for MailerLite featured images */}}
{{- $authorEmail := "" }}
{{- with site.Params.author }}
  {{- if reflect.IsMap . }}
    {{- with .email }}
      {{- $authorEmail = . }}
    {{- end }}
  {{- end }}
{{- else }}
  {{- with site.Author.email }}
    {{- $authorEmail = . }}
    {{- warnf "The author key in site configuration is deprecated. Use params.author.email instead." }}
  {{- end }}
{{- end }}

{{- $authorName := "" }}
{{- with site.Params.author }}
  {{- if reflect.IsMap . }}
    {{- with .name }}
      {{- $authorName = . }}
    {{- end }}
  {{- else }}
    {{- $authorName  = . }}
  {{- end }}
{{- else }}
  {{- with site.Author.name }}
    {{- $authorName = . }}
    {{- warnf "The author key in site configuration is deprecated. Use params.author.name instead." }}
  {{- end }}
{{- end }}

{{- $pctx := . }}
{{- if .IsHome }}{{ $pctx = site }}{{ end }}
{{- $pages := slice }}
{{- if or $.IsHome $.IsSection }}
{{- $pages = $pctx.RegularPages }}
{{- else }}
{{- $pages = $pctx.Pages }}
{{- end }}
{{- $limit := site.Config.Services.RSS.Limit }}
{{- if ge $limit 1 }}
{{- $pages = $pages | first $limit }}
{{- end }}
{{- printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/" xmlns:media="http://search.yahoo.com/mrss/">
  <channel>
    <title>{{ if eq .Title site.Title }}{{ site.Title }}{{ else }}{{ with .Title }}{{ . }} on {{ end }}{{ site.Title }}{{ end }}</title>
    <link>{{ .Permalink }}</link>
    <description>Recent content {{ if ne .Title site.Title }}{{ with .Title }}in {{ . }} {{ end }}{{ end }}on {{ site.Title }}</description>
    {{- with site.Params.images }}
    <image>
      <title>{{ site.Title }}</title>
      <url>{{ index . 0 | absURL }}</url>
      <link>{{ index . 0 | absURL }}</link>
    </image>
    {{- end }}
    <generator>Hugo -- {{ hugo.Version }}</generator>
    <language>{{ site.Language.LanguageCode }}</language>{{ with $authorEmail }}
    <managingEditor>{{.}}{{ with $authorName }} ({{ . }}){{ end }}</managingEditor>{{ end }}{{ with $authorEmail }}
    <webMaster>{{ . }}{{ with $authorName }} ({{ . }}){{ end }}</webMaster>{{ end }}{{ with site.Copyright }}
    <copyright>{{ . | markdownify | plainify | strings.TrimPrefix "© " }}</copyright>{{ end }}{{ if not .Date.IsZero }}
    <lastBuildDate>{{ (index $pages.ByLastmod.Reverse 0).Lastmod.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</lastBuildDate>{{ end }}
    {{- with .OutputFormats.Get "RSS" }}
    {{ printf "<atom:link href=%q rel=\"self\" type=%q />" .Permalink .MediaType | safeHTML }}
    {{- end }}
    {{- range $pages }}
    {{- if and (ne .Layout `search`) (ne .Layout `archives`) }}
    {{- $page := . }}
    {{- $descBody := $page.Description | default $page.Summary }}
    {{- $coverURL := "" }}
    {{- $coverMime := "image/jpeg" }}
    {{- $coverAlt := $page.Title }}
    {{- $coverWidth := "" }}
    {{- $coverHeight := "" }}
    {{- with $page.Params.cover.image }}
      {{- $coverPath := . | strings.TrimPrefix "/" }}
      {{- $coverURL = printf "%s/%s" (strings.TrimSuffix "/" $page.Permalink) $coverPath }}
      {{- with $page.Params.cover.alt }}{{ $coverAlt = . }}{{ end }}
      {{- $ext := path.Ext $coverPath | strings.TrimPrefix "." | lower }}
      {{- if eq $ext "png" }}{{ $coverMime = "image/png" }}{{ else if eq $ext "webp" }}{{ $coverMime = "image/webp" }}{{ else if eq $ext "gif" }}{{ $coverMime = "image/gif" }}{{ end }}
      {{- $imgRes := $page.Resources.GetMatch (path.Base $coverPath) }}
      {{- if not $imgRes }}{{ $imgRes = $page.Resources.GetMatch $coverPath }}{{ end }}
      {{- with $imgRes }}
        {{- $coverWidth = printf "%d" .Width }}
        {{- $coverHeight = printf "%d" .Height }}
      {{- end }}
      {{- /* MailerLite also scans description for the first <img> — prepend cover like WP plugins do */}}
      {{- $descBody = printf "<p><img src=\"%s\" alt=\"%s\" /></p>%s" $coverURL $coverAlt $descBody }}
    {{- end }}
    <item>
      <title>{{ $page.Title }}</title>
      <link>{{ $page.Permalink }}</link>
      <pubDate>{{ $page.PublishDate.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</pubDate>
      {{- with $authorEmail }}<author>{{ . }}{{ with $authorName }} ({{ . }}){{ end }}</author>{{ end }}
      <guid>{{ $page.Permalink }}</guid>
      {{- if $coverURL }}
      <media:content medium="image" url="{{ $coverURL }}" type="{{ $coverMime }}"{{ with $coverWidth }} width="{{ . }}"{{ end }}{{ with $coverHeight }} height="{{ . }}"{{ end }} />
      <media:thumbnail url="{{ $coverURL }}" />
      {{- end }}
      <description>{{ $descBody | html }}</description>
      {{- if and site.Params.ShowFullTextinRSS $page.Content }}
      <content:encoded>{{ (printf "<![CDATA[%s]]>" $page.Content) | safeHTML }}</content:encoded>
      {{- end }}
    </item>
    {{- end }}
    {{- end }}
  </channel>
</rss>
